# Server Watch（Stream Deck プラグイン）仕様書

## これは何か

監視対象のサーバーが生きているかをキーの色で出す。**正常なら緑、異常なら赤。**
押すと、あらかじめ入れておいたコンソールのページ（AWS Lightsail の管理画面など）が開く。

## なぜ作るか

- **見に行かなくても、視界の端で分かる。** 画面を開く動作が消える
- **異常に気づいた次の動作が同じキーに乗っている。** 赤い → 押す → コンソールが開く
- 既存の Ping / Ping Monitor / System Vitals は **いずれも Windows 専用**（2026-09 時点）。
  macOS で、監視基盤を別に建てずに単体で動くものが薄い

## 利用者が入れるもの

**二つだけ。** 多くしない。

| 項目 | 必須 | 例 |
| --- | --- | --- |
| 監視対象 | 必須 | `https://example.com` / `example.com:5432` / `example.com` |
| コンソールのURL | 任意 | `https://lightsail.aws.amazon.com/ls/webapp/home` |

コンソールのURLが空なら、押しても何も起きない。

### 監視対象の書き方で調べ方が決まる

入力の形から判断する。利用者に方式を選ばせない。

| 書き方 | 調べ方 |
| --- | --- |
| `http://` / `https://` で始まる | HTTP で取りに行き、状態番号が 400 未満なら正常 |
| `ホスト:ポート` | TCP で繋がるかどうか |
| それ以外（ホスト名・IP） | ICMP（`/sbin/ping -c 1`） |

**クラウドは ICMP を止めていることが多い。** `https://` で書くのが最も確実で、
設定画面にもそう書いておく。

## 色

| 状態 | 色 | 条件 |
| --- | --- | --- |
| 正常 | 緑 | 直近の確認に成功 |
| 疑い | 黄 | 失敗したが、確定の回数に達していない |
| 異常 | 赤 | 失敗が連続して確定した |
| 待機 | 灰 | まだ一度も確認していない／監視対象が未設定 |

**失敗1回で赤にしない。** 瞬断で赤くなる監視は、赤が意味を失って誰も見なくなる。
既定は3回連続。

## キーの見た目

```
┌────────────┐
│  api-prod    │  ← 表示名（未設定なら対象のホスト名）
│    124ms     │  ← 応答時間。異常時は連続失敗回数
│  ▁▂▃▅▃▂▁▁   │  ← 直近の応答時間の推移
└────────────┘
```

- 異常の間は、応答時間の代わりに **`DOWN 4`**（連続失敗回数）を出す
- 推移の線は直近20回分。**押していない間の履歴を持っていることが、この道具の芯**

## 押した時

- コンソールのURLが入っていれば開く
- 入っていなければ何もしない。**押せない状態が押す前に見えている**ようにする
  （URLが未設定のときはキーの隅に小さく印を出さない＝何も無い、で足りる）

## 設定（Property Inspector）

| 項目 | 既定 |
| --- | --- |
| 監視対象 | （空） |
| 表示名 | （空。空ならホスト名を出す） |
| コンソールのURL | （空） |
| 確認の間隔 | 30秒（最短10秒） |
| 待つ時間 | 5秒 |
| 異常と決める連続失敗回数 | 3 |

## 通信について

**利用者が入れた宛先にだけ繋ぐ。** それ以外のどこにも繋がない。
測定の結果は端末の中だけに置き、外に送らない。

HTTP で調べるときは `HEAD` ではなく `GET` を使い、本文は読まずに閉じる
（`HEAD` を受け付けない相手が珍しくないため）。
`User-Agent` に `StreamDeck-ServerWatch/<版>` を入れる。相手のログで何者か分かるようにする。

## Marketplace の規定に合わせる

- **キーの絵の更新は毎秒10回まで。** この道具は数十秒に1回しか変わらないので余裕だが、
  Combo Counter と同じ関門を通しておく
- 提出の情報は英語。アイコンは 256×256 と 512×512
- SDKVersion 3 / Stream Deck 6.9 以降

## 受け入れ条件

1. 対象を落とすと、確定の回数ぶん確認したのち赤くなる
2. 1回だけ失敗した時は黄で止まり、赤にならない
3. 復旧すると次の確認で緑に戻る
4. 押すとコンソールのURLが開く。未設定なら何も起きない
5. Stream Deck を再起動しても設定が残る
6. 別のページに切り替えて戻しても、その間の確認が欠けていない
7. 監視対象が未設定のときは灰色で、通信を一切しない

## 作る順

| 段階 | 内容 |
| --- | --- |
| 1 | 調べる部分（HTTP / TCP / ICMP）を単体で作り、検査を書く |
| 2 | 状態機械（正常・疑い・異常）と検査 |
| 3 | キーの描画 |
| 4 | Stream Deck との接続 |
| 5 | 設定画面 |
| 6 | 提出用の画像と文面 |

## Combo Counter から持ってくるもの

- 送信の関門（`rate-limit.js`）はそのまま使える
- SVG を `setImage` に直接渡す描き方
- 状態機械を DOM から切り離して検査する組み方
